"""IQP encoder skeleton — Tier 3."""
import numpy as np
from .base import BaseEncoder


class IQPEncoder(BaseEncoder):
    """Diagonal IQP circuits — skeleton only."""

    def encode(self, x: np.ndarray) -> list:
        return []
