"""Custom / FRQI encoder skeleton — Tier 3."""
import numpy as np
from .base import BaseEncoder


class CustomEncoder(BaseEncoder):
    """User-defined encoder registration — skeleton only."""

    def encode(self, x: np.ndarray) -> list:
        return []
